{{#if package.contains_resources}}
mod resource;
use resource::*;
{{/if}}

{{#if package.contains_resources}}
mod bindings;
bindings::export!(Component with_types_in bindings);
{{/if}}

struct Component {}
